<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CarList</title>
</head>
<body>
    <section>
        <% carheading.forEach(function(heading){ %>
            <h1><%= heading.heading %></h1>
        <% }); %>
    </section>
    <ul>
        <% carlist.forEach(function(cars){ %>
          <li><%= cars.name %></li>
        <% }); %>
      </ul>
</body>
</html>